node_modules/
tools/.preview/
__pycache__/
*.pyc
dist/*
!dist/web-tools.js
!dist/app.js
!dist/dictate.js
package-lock.json

# bake-page scratch (skills/bake-page/scripts/bake.js cleans these up; belt and braces)
_bake_src.html
_bake_in.css
_bake_out.css
.concept-index/

# concept-lab derived stores (semantic vectors, term indexes); rebuild, never commit
.concept-lab/
# registerlab fetches the Brysbaert norms on first use; 1.6 MB of survey data
# is a download, not a repo artifact
tools/concept-lab/data/concreteness.tsv

# Debug screenshot dropped by tools/test/playground-pass.mjs (SHOT_PATH overrides)
playground-census.png

# The sheet picker gold set: rebuilt workbooks for someone to open in Excel.
# Regenerated by `npm run gold-set`, byte-reproducible, and derived from source
# workbooks committed in mehrlander/home. Committing it would put binaries a
# commit behind the kit that makes them.
gold-set/
