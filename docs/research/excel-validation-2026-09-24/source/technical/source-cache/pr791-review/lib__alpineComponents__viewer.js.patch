@@ -1185,13 +1185,16 @@ const ViewRegistry = {
           });
 
           let current = -1;
+          let currentTheme = ViewRegistry.SHEET_THEMES.blue;
           const show = (i, opts) => {
             const { key, s } = sheets[i];
-            [...tabs.children].forEach((b, n) => b.classList.toggle('btn-active', n === i));
+            [...tabs.children].forEach((b, n) => {
+              if (b.tagName === 'BUTTON') b.classList.toggle('btn-active', n === i);
+            });
             // Assigned rather than scrollTo(), which jsdom does not implement and
             // which would throw inside the mount the tests drive.
             pane.scrollTop = 0; pane.scrollLeft = 0;
-            pane.replaceChildren(ViewRegistry.drawSheet(s, xl, key, opts));
+            pane.replaceChildren(ViewRegistry.drawSheet(s, xl, key, { ...opts, theme: currentTheme }));
             current = i;
           };
 
@@ -1264,7 +1267,18 @@ const ViewRegistry = {
             return { sheet: here, cell: el.getAttribute('data-c') };
           };
 
-          ViewRegistry.mountSheetTabs(tabs, sheets, show);
+          ViewRegistry.mountSheetTabs(tabs, sheets, show, (theme) => {
+            currentTheme = theme;
+            const table = pane.querySelector('table');
+            if (table) {
+              table.style.setProperty('--xl-th-bg', theme.thBg);
+              table.style.setProperty('--xl-th-text', theme.thText);
+              table.style.setProperty('--xl-th-border', theme.thBorder);
+              table.style.setProperty('--xl-stripe-bg', theme.stripeBg);
+              table.style.setProperty('--xl-subtotal-bg', theme.subtotalBg);
+              table.style.setProperty('--xl-total-border', theme.totalBorder);
+            }
+          });
           ViewRegistry.publishSheets(host, sheets, show, stale, locate);
           show(0);
 
@@ -3090,16 +3104,51 @@ const ViewRegistry = {
 
   // One button per sheet. A sheet the workbook never claimed keeps its part
   // name, which is the honest answer rather than a guessed label.
-  mountSheetTabs(tabs, sheets, show) {
+  mountSheetTabs(tabs, sheets, show, onThemeChange) {
     if (!tabs) return;
-    tabs.replaceChildren(...sheets.map(({ key, s }, i) => {
+    const buttons = sheets.map(({ key, s }, i) => {
       const b = document.createElement('button');
       b.className = 'btn btn-xs';
       b.textContent = s.name || key;
       b.title = `${s.cellCount} cell${s.cellCount === 1 ? '' : 's'}`;
       b.addEventListener('click', () => show(i));
       return b;
-    }));
+    });
+
+    if (onThemeChange) {
+      const themeBox = document.createElement('div');
+      themeBox.className = 'ml-auto flex items-center gap-1.5 text-xs text-base-content/70';
+      themeBox.innerHTML = `
+        <span class="font-medium opacity-60">Theme</span>
+        <select data-xl-theme class="select select-xs select-bordered py-0 h-6 min-h-0 text-xs">
+          <option value="blue" selected>Light Blue (Default)</option>
+          <option value="navy">Navy Blue</option>
+          <option value="teal">Teal</option>
+          <option value="green">Forest Green</option>
+          <option value="dark">Dark Slate</option>
+          <option value="bronze">Bronze</option>
+          <option value="gray">Classic Gray</option>
+          <option value="custom">Custom…</option>
+        </select>
+        <input type="color" data-xl-color value="#c8e1ee" title="Custom color" class="hidden w-5 h-5 p-0 border-0 rounded cursor-pointer">
+      `;
+      const sel = themeBox.querySelector('[data-xl-theme]');
+      const colorInput = themeBox.querySelector('[data-xl-color]');
+      const update = () => {
+        const val = sel.value;
+        colorInput.classList.toggle('hidden', val !== 'custom');
+        const theme = val === 'custom'
+          ? this.themeFromColor(colorInput.value)
+          : (this.SHEET_THEMES[val] || this.SHEET_THEMES.blue);
+        tabs.__currentTheme = theme;
+        onThemeChange(theme);
+      };
+      sel.addEventListener('change', update);
+      colorInput.addEventListener('input', update);
+      tabs.replaceChildren(...buttons, themeBox);
+    } else {
+      tabs.replaceChildren(...buttons);
+    }
     tabs.classList.remove('hidden');
     tabs.classList.add('flex');
   },
@@ -3314,6 +3363,34 @@ const ViewRegistry = {
     dashDotDot: '1px dotted', mediumDashDotDot: '2px dotted', slantDashDot: '2px dashed',
   },
 
+  SHEET_THEMES: {
+    blue:   { thBg: '#c8e1ee', thText: '#000000', thBorder: '#a8cee0', stripeBg: '#f4f9fc', subtotalBg: '#daf0fa', totalBorder: '#000000' },
+    navy:   { thBg: '#104e70', thText: '#ffffff', thBorder: '#0a354d', stripeBg: '#f2f7fa', subtotalBg: '#bce3f5', totalBorder: '#104e70' },
+    teal:   { thBg: '#0d5c58', thText: '#ffffff', thBorder: '#073d3a', stripeBg: '#f0f9f8', subtotalBg: '#bbf0eb', totalBorder: '#0d5c58' },
+    green:  { thBg: '#1e5e3a', thText: '#ffffff', thBorder: '#133d26', stripeBg: '#f1f8f4', subtotalBg: '#c2e8d1', totalBorder: '#1e5e3a' },
+    dark:   { thBg: '#273238', thText: '#ffffff', thBorder: '#181f23', stripeBg: '#f5f7f8', subtotalBg: '#cfd8dc', totalBorder: '#273238' },
+    bronze: { thBg: '#8c4d15', thText: '#ffffff', thBorder: '#5c330e', stripeBg: '#fcf6f0', subtotalBg: '#fde3cc', totalBorder: '#8c4d15' },
+    gray:   { thBg: '#4b5563', thText: '#ffffff', thBorder: '#374151', stripeBg: '#f9fafb', subtotalBg: '#e5e7eb', totalBorder: '#374151' },
+  },
+
+  themeFromColor(hex) {
+    const clean = String(hex || '').trim().replace(/^#/, '');
+    if (!/^[0-9a-f]{6}$/i.test(clean)) return this.SHEET_THEMES.blue;
+    const r = parseInt(clean.slice(0, 2), 16);
+    const g = parseInt(clean.slice(2, 4), 16);
+    const b = parseInt(clean.slice(4, 6), 16);
+    const brightness = (r * 299 + g * 587 + b * 114) / 1000;
+    const thText = brightness > 150 ? '#000000' : '#ffffff';
+    return {
+      thBg: '#' + clean,
+      thText,
+      thBorder: '#' + clean,
+      stripeBg: '#' + clean + '12',
+      subtotalBg: '#' + clean + '38',
+      totalBorder: thText === '#000000' ? '#000000' : '#' + clean,
+    };
+  },
+
   // A colour only where the workbook wrote one this can vouch for. Anything
   // else is dropped rather than guessed, which keeps a malformed palette from
   // reaching a style attribute.
@@ -3375,6 +3452,8 @@ const ViewRegistry = {
       return wrap;
     }
 
+    const theme = opts?.theme || this.SHEET_THEMES.blue;
+
     // Scoped per mount, since a page can hold three viewers and each workbook
     // numbers its styles from zero.
     const uid = 'sh' + Math.random().toString(36).slice(2, 8);
@@ -3396,7 +3475,9 @@ const ViewRegistry = {
 
     const rules = [
       `.${uid}{border-collapse:collapse;table-layout:fixed;font-size:14.7px;` +
-        `font-family:Calibri,Aptos,'Segoe UI',system-ui,sans-serif;background:#fff;color:#000}`,
+        `font-family:Calibri,Aptos,'Segoe UI',system-ui,sans-serif;background:#fff;color:#000;` +
+        `--xl-th-bg:${theme.thBg};--xl-th-text:${theme.thText};--xl-th-border:${theme.thBorder};` +
+        `--xl-stripe-bg:${theme.stripeBg};--xl-subtotal-bg:${theme.subtotalBg};--xl-total-border:${theme.totalBorder}}`,
       `.${uid} td{border:1px solid #e3e3e3;padding:1px ${this.SHEET_PAD_X}px;overflow:hidden;white-space:nowrap;` +
         `vertical-align:bottom;line-height:1.2;text-align:left}`,
       `.${uid} td.n{text-align:right}`,
@@ -3405,6 +3486,18 @@ const ViewRegistry = {
       `.${uid} thead th{top:0;height:20px}`,
       `.${uid} tbody th{left:0;width:${gutter}px;z-index:1}`,
       `.${uid} thead th:first-child{left:0;z-index:3}`,
+      `.${uid} td.tbl-hdr, .${uid} td.pvt-hdr{background:var(--xl-th-bg,#c8e1ee)!important;` +
+        `color:var(--xl-th-text,#000000)!important;font-weight:600;border-color:var(--xl-th-border,#a8cee0)!important}`,
+      `.${uid} td.tbl-hdr a, .${uid} td.pvt-hdr a{color:var(--xl-th-text,#000000)!important}`,
+      `.${uid} td.tbl-stripe{background:var(--xl-stripe-bg,#f4f9fc)}`,
+      `.${uid} td.pvt-subtotal{background:var(--xl-subtotal-bg,#daf0fa)!important;font-weight:600}`,
+      `.${uid} td.tbl-total, .${uid} td.pvt-grandtotal{font-weight:700!important;` +
+        `border-top:1px solid var(--xl-total-border,#000)!important;` +
+        `border-bottom:3px double var(--xl-total-border,#000)!important}`,
+      `.${uid} .xl-caret{display:inline-block;margin-left:5px;font-size:10px;opacity:.9;vertical-align:baseline}`,
+      `.${uid} .xl-toggle{display:inline-flex;align-items:center;justify-content:center;` +
+        `width:11px;height:11px;border:1px solid #777;font-size:9px;line-height:1;margin-right:5px;` +
+        `background:#fff;color:#333;user-select:none;border-radius:1px;vertical-align:middle}`,
       // An anchored picture is positioned inside its own cell rather than over
       // the table, so it needs no arithmetic about the header or the gutter and
       // it stays put when a row above it is hidden.
@@ -3497,6 +3590,8 @@ const ViewRegistry = {
         if (c.numeric) classes.push('n');
         if (c.style != null) classes.push('s' + c.style);
         if (c.cf != null) classes.push('cf' + c.cf);
+        if (c.tableRole) classes.push(c.tableRole);
+        if (c.pvtRole) classes.push(c.pvtRole);
         // A comment outranks a list, which outranks an instruction: one mark
         // per cell, the strongest thing it carries. All three texts still go
         // into the note.
@@ -3587,6 +3682,12 @@ const ViewRegistry = {
           if (href) text = `<a href="${esc(href)}" target="_blank" rel="noopener"${tip}>${text || esc(href)}</a>`;
           else if (c.link.location) text = `<a href="#" data-goto="${esc(c.link.location)}"${tip}>${text}</a>`;
         }
+        if (c.hasHierarchyToggle) {
+          text = `<span class="xl-toggle">−</span>` + text;
+        }
+        if (c.hasFilter) {
+          text = text + `<span class="xl-caret">▾</span>`;
+        }
         let cellStyle = stick.join(';');
         if (c.spillLeft || c.spillRight) {
           const px = (from, count) => layout.cols.slice(from, from + count)
