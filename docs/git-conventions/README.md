# git-conventions

Short, durable notes on how this repo treats git history and branches — the
reasoning behind the workflow rules in [CLAUDE.md](../../CLAUDE.md), kept here
so the rules have a place to point.

- **[post-merge-branch-mutation.md](post-merge-branch-mutation.md)** — why a
  merged branch should stop being a live workspace. Necromerging, zombie
  branches, and the rule of thumb: *merged means closed.*
