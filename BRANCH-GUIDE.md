# Branch guide: claude/dreamy-pascal-luo2b0

Polishing the `pages/` landing page, adding a "lead with the live view" README convention, and giving web-tools a project mark: a slot-split hex nut (tools + the `<` `>` of the web + hexagonal modularity), wired in as the default favicon for loader pages.

⭐ [lib/favicon.svg (this branch)](https://github.com/mehrlander/web-tools/blob/claude/dreamy-pascal-luo2b0/lib/favicon.svg) — the mark. Page-markup changes preview via screenshot, not the live URL, since `?use=` swaps loaded lib code, not a page's own HTML.

**Changed:**
- lib/favicon.svg — new project mark (slot-split hex nut, blue-600 #2563eb).
- lib/gh-boot.js — injects the mark as the favicon on loader pages that don't set their own (bespoke favicons win; pages index keeps its grid).
- README.md — leads with ⭐ live index link; hex nut in the H1.
- tools/build/pages-index.mjs — generator: grid favicon + inline logo, `show-repo` label, pre-rendered caption, README lead link.
- pages/favicon.svg — grid mark (kept as the pages-index identity).
- pages/index.html + pages/README.md — regenerated catalogs.
- docs/CONVENTIONS.md — "Lead with the live view" surfacing primitive.
- dist/web-tools.js — pre-build, regenerated by the commit hook (lib changed).

**Next steps / open threads:**
- Standalone pages that don't boot the loader (e.g. diff-tool) won't get the default favicon; add a `<link>` to those if desired.
- jsDelivr caches main's gh-boot.js ~12h, so the default favicon goes live after merge + cache expiry (or a purge); preview now via `?use=<sha>`.
- At wrap-up: fold this guide into docs/MERGE-GUIDE.md and delete it.
